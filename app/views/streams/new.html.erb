<%= simple_form_for @stream do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', :f => f %>
  <%= f.button :submit %> <span class="back_link"><%= link_to "cancel", :back %></span>
<% end %>