<%= f.simple_fields_for :events do |e| %>
  <%= e.input :name, :label => "Event" %>
  <!-- TODO: make stream an input field -->
  <%= e.association :stream %>
  <!-- TODO: redo date and time selectors -->
  <%= e.input :date %>
  <%= e.input :time %>
<% end %>