<%= simple_form_for @stream do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= f.input :link %>
  <%= f.association :games %>
  <%= f.association :streamers %>
  <%= f.button :submit %>  
<% end %>