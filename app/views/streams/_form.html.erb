<%= simple_form_for @stream do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= f.inputs :link, :description, :streamers, :games, :live, :platform, :channel_id %>
  <%= f.actions %>
<% end %>