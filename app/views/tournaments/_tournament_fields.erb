<%= f.input :link, :label => "Tournament Link" %>
<!-- TODO: Change select box to string with autocomplete -->
<%= f.association :game, :as => :select %>
<%= f.input :name, :label => "Tournament Name" %>
<%= f.input :start_date %>