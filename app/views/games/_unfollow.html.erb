<%= form_for(current_user.followed_games.find_by_game_id(@game),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= button_tag(:type => 'submit', :class => 'btn btn-danger') do %>
      <i class="icon-white icon-eye-close"></i> Unfollow
    <% end %>
<% end %>