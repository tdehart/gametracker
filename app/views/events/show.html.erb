<%= link_to "<< back", :back %>
<p><%= @event.tournament.name %></p>
<p><%= @event.name %></p>
<p><%= @event.event_time %></p>
<p><%= @event.description %></p>
<% @event.streams.each do |e| %>
  <%= e %>
<% end %>
<div>
  <%= link_to "Edit", edit_event_path(@event) %>
  <%= link_to 'Destroy', @event, method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
