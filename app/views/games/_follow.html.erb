<%= form_for(current_user.followed_games.build(game_id: @game.id), remote: true) do |f| %>
    <div><%= f.hidden_field :game_id %></div>
    <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>