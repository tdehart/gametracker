<%= form_for(current_user.followed_tournaments.find_by_tournament_id(@tournament),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>